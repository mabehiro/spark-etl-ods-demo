apiVersion: config.openshift.io/v1
kind: OAuth
metadata:
  name: cluster
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  identityProviders:
    - htpasswd:
        fileData:
          name: htpasswd-pass
      mappingMethod: claim
      name: htpasswd
      type: HTPasswd
