apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: spark-application-servicemonitor
  namespace: spark-etl-project
spec:
  endpoints:
    - path: /metrics
      interval: 30s
      port: metrics
      scheme: http
  selector:
    matchLabels:
      spark/servicemonitor: "true"